; Autogenerated by puppet. Do not edit.

[default]
; Default priority (lower value means higher priority)
priority = <%= scope['f3backup::priority'] %>

; Different types of backup. Will be done in the same order as specified here.
; Valid options: rdiff-backup, command
backups = <% if scope['f3backup::backup_rdiff'] %>rdiff-backup, <% end %><% if scope['f3backup::post_command'] %>command<% end %>
<% if scope['f3backup::backup_rdiff'] -%>

[rdiff-backup]
<% if scope['f3backup::rdiff_global_exclude_file'] -%>
global-exclude-file = <%= scope['f3backup::rdiff_global_exclude_file'] %>
<% end -%>
<% if scope['f3backup::rdiff_user'] -%>
user = <%= scope['f3backup::rdiff_user'] %>
<% end -%>
<% if scope['f3backup::rdiff_path'] -%>
path = <%= scope['f3backup::rdiff_path'] %>
<% end -%>

; Optional extra parameters for rdiff-backup
extra-parameters = <%= scope['f3backup::rdiff_extra_parameters'] %>

; How long backups are going to be kept
keep = <%= scope['f3backup::rdiff_keep'] %>
<% end -%>
<% if scope['f3backup::post_command'] -%>

[command]
; Run a specific command on the backup server after the backup has finished
command = <%= scope['f3backup::post_command'] %>
<% end -%>

